sampler2D tex : register(s0);

struct PSInput
{
	float2 TexCoord : TEXCOORD0;
};

struct PSOutput
{
    float4 Color : COLOR;
};

PSOutput main(PSInput ip)
{
    PSOutput o;

	//float e = log2(color.r * 0.3 + color.g * 0.59 + color.b * 0.11 + .75);
	//if (e >= 0)
	//{
	//	return e * e;
	//}
	//return 0;
	float3 color = tex2D(tex, ip.TexCoord).rgb;
	float e = dot(color, float3(0.3, 0.59, 0.11)); //color.r * 0.3 + color.g * 0.59 + color.b * 0.11;;
	if (e >= 1)
	{
		return 1
	}
	return 0;
}
